# MediaPipe model

Download the MediaPipe BlazeFace short-range model and save it as `blaze_face_short_range.tflite` in this directory before using the selfie step. The application deliberately loads this model from the same origin so camera images and frames never leave the browser.
